# shujuan migrations

This directory is the tracked migration history for shujuan project databases.

Use numbered `.sql` files here when a future schema change must migrate existing databases. The ignored `.shujuan/migrations/` directory is reserved for local runtime artifacts and is not a source of schema history.
